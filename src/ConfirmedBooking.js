import React from "react";

function ConfirmedBooking(){
    return(
        <main>
            <p class="confirm_title">Table reservation completed successfully!</p>
            <p class="confirm_text">We are anxiously waiting for your visit.</p>
        </main>
    );
}

export default ConfirmedBooking;